describe('Test', () => {
  it('should return true', () => {
    // ASSIGN
    const expected = true;

    // ACT
    const actual = true;

    // ASSERT
    expect(actual).toEqual(expected);
  });
});